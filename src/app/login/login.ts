import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  imports: [],
  templateUrl: './login.html',
  // template: `<h1>Login Component</h1>`, // Alternative inline template
  styleUrl: './login.css',
})
export class Login {

}
 //ng generate component login --skip-tests
 //ng g c login --skip-tests
